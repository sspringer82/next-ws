export type Product = {
  id: number;
  name: string;
  description: string;
  price: number;
  size: string;
  weight: number;
  image: string;
};

export type User = {
  id: string;
  name: string;
  email: string;
  password: string;
};
